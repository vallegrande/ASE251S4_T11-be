apiVersion: v1
kind: Service
metadata:
  name: agrimarket-service
  namespace: brando-lucana-agrimarket-be
spec:
  type: NodePort
  selector:
    app: agrimarket-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
